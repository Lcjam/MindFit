spring:
  profiles:
    active: dev
  application:
    name: mindfit

server:
  port: 8080

app:
  cors:
    # CORS 허용 origin (콤마로 여러 개 지정 가능). 시크릿이 아니므로 base 설정에 기본값을 둔다.
    # 환경별 override는 application-dev.yml / application-prod.yml(미커밋) 또는 환경변수로 한다.
    allowed-origins: http://localhost:5173
